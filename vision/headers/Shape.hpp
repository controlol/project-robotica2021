#ifndef shapeheader
#define shapeheader
class Shape
{
private:
    /* data */
    //list of pixels
public:
    Shape(/* args */);
    ~Shape();

    bool IsShapeSquare();
    bool IsBiggestShape();
};
#endif

